// Lerp scroll removed — replaced by CSS scroll-snap (scroll-snap-type: y mandatory)
// CSS snap is more reliable, works with IntersectionObserver,
// doesn't conflict with touch/keyboard, and requires zero JS.
export function initLerpScroll() {}
